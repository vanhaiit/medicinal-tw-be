export enum UserStatus {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
}

export enum UserRole {
    USER = 'USER',
    ADMIN = 'ADMIN',
    SUPER_ADMIN = 'SUPER_ADMIN',
}

export enum UserIsLocked {
    LOCKED = 'LOCKED',
    UNLOCKED = 'UNLOCKED',
}

export enum UserType {
    employee = 'employee',
    customer = 'customer',
}
